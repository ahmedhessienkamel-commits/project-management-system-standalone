# خطة النقل الخارجي وقاعدة البيانات وتسجيل الدخول المحلي

## القرار المقترح

الاختيار الأنسب لهذا النظام عند تشغيله خارجيًا هو **DigitalOcean App Platform مع MySQL مُدار**، لأن التطبيق يحتاج خادم Node.js/Express وقاعدة بيانات علائقية دائمة، ولأن قاعدة البيانات المُدارة تضيف فصلًا تشغيليًا عن التطبيق وخيارات وصول موثوق بها. منصة Railway مناسبة إذا كانت الأولوية لبدء نقل سريع وبسيط، بينما Render بديل جيد عندما تُراد خدمة Express مستقلة مع خدمة MySQL خاصة. تدعم المصادر الرسمية هذه التركيبات الثلاثة. [1] [2] [3] [4]

| الخيار | متى يُفضّل | قاعدة البيانات | مستوى الإدارة |
|---|---|---|---|
| DigitalOcean App Platform + Managed MySQL | الإنتاج طويل الأجل والبيانات المحاسبية | MySQL مُدار مع مصادر موثوقة واتصال داخلي | الأقل من حيث إدارة الخوادم |
| Railway + MySQL | تجربة نقل أولية أو فريق صغير | خدمة MySQL ضمن المشروع | بسيط وسريع، مع ضرورة وضع نسخ احتياطي مستقل |
| Render + Private MySQL | خدمة Express منفصلة مع إعداد واضح للقرص | MySQL 8 كخدمة خاصة مع قرص دائم | متوسط؛ يلزم الانتباه للنسخ المنطقي |

> لا تعتمد على لقطة قرص فقط لاستعادة MySQL. وثائق Render نفسها توصي بأداة النسخ الاحتياطي المنطقية مثل `mysqldump` لتجنب مخاطر فقد أو تلف البيانات. [2]

## إعداد قاعدة بيانات مستقلة

1. أنشئ **قاعدة MySQL 8 جديدة ومُدارة** في منطقة قريبة من المستخدمين، وأنشئ مستخدم تطبيق محدود الصلاحيات بدل حساب الجذر.
2. اجعل الوصول مقتصرًا على خادم التطبيق عبر شبكة خاصة أو قائمة عناوين موثوقة، وفعّل TLS إذا قدمه مزود القاعدة.
3. أنشئ كلمة مرور طويلة وفريدة، ثم احفظ سلسلة الاتصال `DATABASE_URL` في مدير أسرار الاستضافة، ولا تحفظها في الكود أو Git.
4. فك ضغط حزمة المصدر في مستودع خاص، ثم ثبّت التبعيات:

   ```bash
   pnpm install --frozen-lockfile
   pnpm exec tsc --noEmit
   pnpm test -- --run
   ```

5. راجع ترحيلات `drizzle/` على قاعدة اختبار فارغة أولًا، ثم طبّقها على القاعدة الجديدة:

   ```bash
   pnpm drizzle-kit migrate
   ```

6. انقل بيانات التشغيل فقط بعد أخذ نسخة احتياطية معتمدة واختبار استعادتها على قاعدة منفصلة. إذا كنت تملك بيانات اتصال قاعدة المصدر ومصرحًا لك باستخدامها، استخدم نسخة منطقية مشفرة مثل:

   ```bash
   mysqldump --single-transaction --routines --triggers --databases YOUR_DATABASE > backup.sql
   mysql --ssl-mode=REQUIRED -h NEW_HOST -u NEW_USER -p NEW_DATABASE < backup.sql
   ```

7. نفّذ فحصًا بعد الاستعادة: عدد المشاريع، المستخدمين، الأرصدة، فواتير المبيعات، المستخلصات، وطلبات المواد، ثم لا تغيّر مصدر البيانات التشغيلي حتى يوقّع المالك على نتائج الفحص.

## متغيرات البيئة الخارجية

استخدم ملف `EXTERNAL_ENVIRONMENT_TEMPLATE.txt` كنموذج **للأسماء فقط**. تحتاج على الأقل إلى:

| المتغير | الغرض |
|---|---|
| `DATABASE_URL` | الاتصال بقاعدة MySQL الجديدة. |
| `JWT_SECRET` | توقيع جلسات الدخول المحلية؛ يجب إنشاؤه من جديد وبطول عشوائي قوي. |
| `APP_URL` | النطاق النهائي للتطبيق ويُستخدم في روابط الاستعادة والدعوات. |
| `GMAIL_USERNAME` و`GMAIL_APP_PASSWORD` أو مزود SMTP بديل | إرسال دعوات واستعادة كلمات المرور. |
| إعدادات مزود الهوية، إن اختير OIDC | تستخدم فقط إن لم تعتمد الدخول المحلي. |

## تحويل تسجيل الدخول إلى محلي

يمكنني تنفيذ ذلك. توجد في المصدر الحالي قاعدة جيدة للدخول المحلي: كلمات المرور تُجزّأ بـ `scrypt`، والجلسة المحلية موقعة بـ JWT، ويوجد مسار لدعوات المستخدمين واستعادة كلمة المرور. لكن الإطلاق الخارجي يحتاج فصل المسارات التي تعتمد على المصادقة المدمجة حتى لا يستدعي التطبيق SDK خارجيًا في كل طلب.

| مرحلة البرمجة | التعديل المطلوب |
|---|---|
| 1. وضع مصادقة | إضافة `AUTH_MODE=local` وتعطيل مسار SDK المدمج عندما يكون الوضع محليًا. |
| 2. سياق الجلسة | اعتماد كوكي جلسة كلمة المرور المحلية فقط، مع `Secure` و`HttpOnly` و`SameSite=Lax` في الإنتاج. |
| 3. المالك الأول | إنشاء أمر تهيئة لمرة واحدة أو دعوة مالك أول؛ يمنع إنشاء حساب مدير بلا مفتاح إعداد محمي. |
| 4. حماية الدخول | معدل محاولات الدخول والاستعادة، سجل تدقيق، والتحقق من أصل الطلبات ذات الجلسة. |
| 5. البريد | ربط SMTP خارجي لاختبار دعوات المستخدمين وروابط الاستعادة على نطاقك النهائي. |
| 6. الاختبار | اختبار تسجيل الدخول والخروج والاستعادة والدعوات والأدوار على قاعدة اختبار قبل أي نقل إنتاجي. |

## ما أحتاجه قبل تنفيذ النسخة الخارجية

أحتاج منك اختيار منصة الاستضافة أولًا، ثم توفير نطاقك النهائي أو السماح باستخدام نطاق مؤقت، وبيانات SMTP إن أردت تفعيل الاستعادة البريدية في نفس المرحلة. لا ترسل كلمات المرور في الدردشة؛ توضع لاحقًا في مدير أسرار الاستضافة.

## المراجع

[1] [Render: Deploy a Node Express App](https://render.com/docs/deploy-node-express-app)

[2] [Render: Deploy MySQL](https://render.com/docs/deploy-mysql)

[3] [Railway: Deploy MySQL](https://railway.com/deploy/mysql--mysql)

[4] [DigitalOcean: Manage Databases in App Platform](https://docs.digitalocean.com/products/app-platform/how-to/manage-databases/)
