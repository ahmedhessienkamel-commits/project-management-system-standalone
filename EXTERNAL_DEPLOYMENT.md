# حزمة النقل المستقلة لنظام إدارة المشاريع

## النطاق

هذه الحزمة هي **مصدر ERP مستقل قابل للتشغيل** على VPS أو خادم سحابي أو Railway. تشمل الواجهة والخادم ومخطط Drizzle والترحيلات وملفات Docker والتوثيق، ولا تشمل بيانات الإنتاج أو كلمات المرور أو مفاتيح البيئة أو سجلات التشغيل أو `node_modules`.

> لا تحفظ `.env` أو كلمات المرور أو مفاتيح SMTP أو سلاسل اتصال قاعدة البيانات في Git. استخدم مدير أسرار مزود الاستضافة، واحفظ النسخ الاحتياطية في موقع منفصل ومشفر.

## ما تتضمنه الحزمة

| العنصر | الحالة | الملاحظة |
|---|---:|---|
| واجهة React وTailwind | مشمول | تُبنى بواسطة Vite ضمن `dist/public`. |
| خادم Express وtRPC | مشمول | يعمل على Node.js 22. |
| مخطط Drizzle والترحيلات | مشمول | موجه إلى MySQL 8 أو MySQL متوافق. |
| تسجيل الدخول المحلي | مشمول | جلسة JWT آمنة، دعوات، واستعادة كلمة المرور. |
| بريد SMTP | مشمول اختياريًا | لا يعتمد على مزود محدد؛ مطلوب فقط للرسائل البريدية. |
| Docker للتشغيل | مشمول | `Dockerfile` الجذري للاستضافات السحابية وCompose/Caddy ضمن `deployment/` لخادم VPS. |
| بيانات التشغيل الحقيقية | غير مشمولة | تنقل فقط بقرار صريح من مالكها وبعد اختبار استعادة. |
| الخدمات المدمجة السابقة | غير مشمولة | OAuth والتخزين المدمج والقدرات الذكية والإشعارات المدمجة أزيلت أو عُطلت. |

## المتطلبات

يحتاج النظام إلى Node.js 22 وقاعدة MySQL 8 أو خدمة متوافقة وسر `JWT_SECRET` جديد. استخدم HTTPS في الإنتاج، واضبط `APP_URL` على النطاق النهائي حتى تكون روابط الدعوات واستعادة كلمة المرور صحيحة.

## تشغيل محلي أو على VPS

1. انسخ المصدر إلى مستودع خاص ثم نفذ `pnpm install --frozen-lockfile`.
2. انسخ `EXTERNAL_ENVIRONMENT_TEMPLATE.txt` إلى ملف بيئة محلي خاص، واضبط `DATABASE_URL` و`JWT_SECRET` و`SCHEDULE_SECRET` و`APP_URL`.
3. أنشئ قاعدة MySQL فارغة ثم نفذ `pnpm db:migrate`.
4. أنشئ المالك المحلي عبر `pnpm local:bootstrap-admin owner@example.com 'كلمة-مرور-قوية'` داخل الطرفية الخاصة بالخادم فقط.
5. نفذ `pnpm exec tsc --noEmit` و`pnpm test -- --run` ثم `pnpm build`.
6. شغّل التطبيق بـ`NODE_ENV=production pnpm start` خلف وكيل HTTPS، أو اتبع `deployment/README.md` لتشغيل Docker Compose وCaddy.

## البريد والنسخ الاحتياطي

اضبط متغيرات `SMTP_HOST` و`SMTP_PORT` و`SMTP_SECURE` و`SMTP_USER` و`SMTP_PASSWORD` و`SMTP_FROM` إن أردت الدعوات واستعادة كلمة المرور والتنبيهات. النسخ الاحتياطية البريدية اختيارية وتتطلب `BACKUP_EMAIL_RECIPIENT` إضافةً إلى SMTP؛ لن يرسل النظام أي نسخة بالبريد دون تشغيل endpoint الجدولة المحمي.

## نقل البيانات

لا تتضمن الحزمة تصديرًا تلقائيًا للبيانات الفعلية. ابدأ بقاعدة بيانات فارغة وأنشئ المالك المحلي، أو نفّذ تصديرًا صريحًا بعد تحديد البيانات المطلوبة وتأكيد المالك. يجب دائمًا اختبار الاستعادة على قاعدة منفصلة قبل لمس قاعدة الإنتاج. راجع `INDEPENDENT_BACKUP_RESTORE.md` للتفريق بين النسخة الكاملة لـMySQL وتصدير JSON الآمن الذي يستثني كلمات المرور والرموز.

## حدود مقصودة

لا تعمل روابط المرفقات التاريخية التي تعتمد على `/manus-storage/` خارجيًا. لم يعد النظام يتصل بـOAuth أو Forge أو التخزين المدمج. إذا احتجت رفع ملفات جديدة أو ترحيل ملفات قديمة، أضف تخزين S3 متوافقًا قبل تفعيل هذه الخاصية.

## Railway

تشمل الحزمة دليلًا محددًا في `RAILWAY_DEPLOYMENT.md` يوضح إضافة التطبيق وMySQL وربط `DATABASE_URL` داخليًا وإنشاء حساب المالك والتحقق من `/health` و`/login` بعد النشر.
