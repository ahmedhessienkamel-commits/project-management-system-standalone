# حزمة نقل نظام إدارة المشاريع

هذه الحزمة هي **نسخة من كود المصدر فقط** قابلة للنقل إلى خادم خارجي. لا تشمل الحزمة قاعدة بيانات الإنتاج، الجلسات، كلمات المرور، مفاتيح البيئة، سجلات التشغيل، أو تبعيات `node_modules`.

> لا ترفع ملف `.env` أو أي كلمة مرور أو رمز دخول إلى مستودع عام. استخدم مدير أسرار لدى مزود الاستضافة، واحفظ النسخ الاحتياطية المشفرة في مكان منفصل.

## ما تتضمنه الحزمة

| العنصر | الحالة | الملاحظة |
|---|---:|---|
| واجهة React وTailwind | مشمول | تُبنى مع Vite. |
| خادم Express وtRPC | مشمول | يتطلب Node.js 22 أو أحدث. |
| مخطط Drizzle والترحيلات | مشمول | يُطبّق على MySQL 8 أو TiDB متوافق. |
| تكامل البريد | مشمول | يحتاج متغيرات SMTP خاصة بك. |
| قاعدة البيانات الفعلية | غير مشمولة | يجب تصديرها بأداة النسخ الاحتياطي الخاصة بقاعدة بياناتك وبموافقة مالكها. |
| تسجيل دخول Manus المدمج | غير قابل للنقل كما هو | يجب استبداله بمزود هوية خارجي أو تسجيل الدخول بكلمة المرور الموجود في المشروع بعد مراجعته. |
| مفاتيح المنصة والجلسات | غير مشمولة | لا تنسخها إلى استضافة خارجية. |

## متطلبات الاستضافة الخارجية

استخدم خادم Node.js مع **Node.js 22+** وقاعدة **MySQL 8+ أو TiDB**. يحتاج الخادم إلى وصول مشفر لقاعدة البيانات وإلى مدير أسرار يضبط متغيرات البيئة عند التشغيل. يمكن تشغيله على خادم Linux، أو منصة Node.js مُدارة تدعم تطبيق Express دائم، أو حاوية Docker بعد إعداد مزود الهوية وقاعدة البيانات.

## خطوات الإعداد الأساسية

1. فك ضغط الحزمة في مستودع خاص، ثم نفّذ `pnpm install --frozen-lockfile`.
2. انسخ `.env.example` إلى `.env` محليًا فقط، واستبدل القيم الوهمية بقيم البيئة الخارجية.
3. أنشئ قاعدة بيانات جديدة، ثم راجع كل ملف ضمن `drizzle/` قبل تطبيق الترحيلات. لا تطبّق ترحيلات على قاعدة إنتاج دون نسخة احتياطية واختبار مسبق.
4. نفّذ `pnpm exec tsc --noEmit` ثم `pnpm test -- --run` للتحقق من المصدر.
5. ابنِ التطبيق بـ `pnpm build`، ثم شغّله بـ `NODE_ENV=production pnpm start` خلف وكيل HTTPS يدير النطاق والشهادات.

## تسجيل الدخول بعد النقل

النسخة الحالية تعتمد على تسجيل دخول مدمج في المنصة؛ لذلك لن يعمل هذا الجزء خارجيًا من دون تكيف. قبل الإطلاق الخارجي يجب اختيار أحد المسارين التاليين:

| المسار | ما يلزم |
|---|---|
| تسجيل دخول محلي بكلمة مرور | مراجعة وتفعيل مسارات كلمة المرور، جلسات آمنة، استعادة كلمة المرور، وتكوين SMTP. |
| مزود هوية خارجي | إعداد OAuth/OIDC (مثل Auth0 أو Keycloak أو مزود المؤسسة)، ثم استبدال مكونات المصادقة المدمجة وتسجيل عناوين callback للنطاق الجديد. |

## نقل البيانات

لا تتضمن الحزمة أي تصدير لبيانات التشغيل. يجب إنشاء نسخة احتياطية من قاعدة البيانات من خلال مالك قاعدة البيانات أو واجهة إدارتها، ثم اختبار الاستعادة إلى قاعدة منفصلة. عند النقل، انقل البنية والبيانات ضمن قناة مشفرة، وبدّل كلمات مرور قاعدة البيانات ومفاتيح الجلسات بعد نجاح الاستعادة.

## ما يجب اختباره قبل الإطلاق

- تسجيل الدخول والخروج واستعادة كلمة المرور.
- قيود الأدوار، خصوصًا المالك ومسؤول المشتريات والمدير العام.
- قراءة وكتابة المعاملات المحاسبية على قاعدة بيانات اختبارية.
- إرسال البريد والتنبيهات إن تم تفعيلها.
- اتصال الموقع بالنطاق HTTPS ومسارات SPA وتوجيه `/api/*` إلى خادم Express.

## ملاحظة حول الدعم

هذه الحزمة تتيح لك امتلاك نسخة مصدر مستقلة، لكنها لا تحوّل تسجيل الدخول أو قاعدة البيانات تلقائيًا. يلزم تنفيذ تهيئة خارجية منفصلة عند اختيار مزود الاستضافة والهوية.
