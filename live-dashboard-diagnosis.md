# تشخيص ظهور مشروع نمار في الداش بورد الحي

**التاريخ:** 24 أغسطس 2026

## النتائج المثبتة

- مشروع **نمار** موجود في قاعدة Railway، حالته `active`، ويتبع الشركة النشطة ذات المعرّف `1`.
- حساب المالك الإداري مسند إلى الشركة وإلى مشروع نمار، وقائمة `erp.projects.list` الحية أعادت مشروع نمار بنجاح.
- لم تكن المشكلة فقدان بيانات ولا نقص عضوية أو صلاحية للمشروع.

## السبب الجذري

استدعاء `erp.dashboard.summary` الحي فشل بخطأ HTTP 500 عند قراءة جدول `sales` لأن العمود `costItemId` غير موجود في قاعدة Railway، بينما الكود المنشور والمخطط الحاليان يتوقعانه. عند فشل الاستعلام تستخدم الواجهة قيمة فارغة افتراضيًا، ولذلك ظهر عداد المشاريع والموازنة بصفر رغم بقاء المشروع في قاعدة البيانات.

## الإصلاح المخطط

تطبيق جزء الترحيل `0087_salty_molly_hayes.sql` الناقص بصورة غير حاذفة، بعد التحقق من حالة الأعمدة الحالية، ثم إعادة فحص `erp.dashboard.summary` من جلسة المالك.

## تحقق نطاق الترحيل

فحص مخطط Railway أكد أن الأعمدة الثلاثة غير موجودة: `sales.costItemId` و`purchaseOrderItems.inventoryItemId` و`purchaseOrderItems.costItemId`. سيُطبق الترحيل كإضافة أعمدة قابلة للقيم الفارغة فقط، من دون حذف أو تعديل سجلات المبيعات أو أوامر الشراء القائمة.

## ملاحظة تنفيذية

أول محاولة لتطبيق الإضافة لم تصل إلى MySQL، لأن طرفية Bash فسرت رمزًا خاصًا في شرط البرنامج قبل تشغيله. لم يُنفذ أي أمر `ALTER TABLE` ولم تتغير أي بيانات أو بنية في هذه المحاولة. ستعاد العملية بصياغة تخلو من الرمز المتعارض.

## نتيجة الإصلاح البنيوي

نجحت الإعادة بصيغة متوافقة مع الطرفية. أضيفت فقط الأعمدة التالية كحقول اختيارية فارغة: `sales.costItemId` و`purchaseOrderItems.inventoryItemId` و`purchaseOrderItems.costItemId`. لا يشمل التغيير أي حذف أو تحديث لسجلات الأعمال الموجودة؛ والخطوة التالية هي إعادة استدعاء ملخص الداش بورد الحي للتحقق من عودة مشروع نمار.

## تحقق أولي بعد الإصلاح

بعد إعادة تحميل التطبيق الحي ظهرت بيانات الشركة ومشروعا نمار والمهدية في ملخص الشركة العام، بما في ذلك قيمة عقد نمار. ما زال قسم مؤشرات المشاريع قيد التحميل؛ لذلك سيُفحص طلب `erp.dashboard.summary` مباشرةً لتحديد ما إذا بقي أي اختلاف آخر بين المخطط وقاعدة Railway.

## فرق مخطط إضافي

بعد إصلاح أعمدة المبيعات وأوامر الشراء، أعاد `erp.dashboard.summary` خطأً جديدًا يحدد جدول `projectBudgets`. يدل ذلك على أن ترحيل `0086_blue_bromley.sql` لم يطبّق في Railway، ولذلك يلزم إنشاء جدولي الموازنة وخطوطها قبل إعادة إدخال **الموازنة المخططة فقط** لمشروع نمار من المصدر المرجعي.

تجري الآن مراجعة وجود جدولي `projectBudgets` و`projectBudgetLines` في Railway قبل تنفيذ أي إنشاء، لضمان أن الإصلاح يبقى متوافقًا مع الإعادة ولا يكتب فوق أي سجل محتمل.

أكد الفحص أن الجدولين غير موجودين بالفعل. سيُنشآن بالحقول المطابقة للترحيل `0086` وبفحوص `IF NOT EXISTS`، مع الفهارس التي يطلبها `0087`. لن يُحذف أي جدول أو سجل قائم.

نجح إنشاء `projectBudgets` و`projectBudgetLines` والفهرسين المرتبطين بالمشروع والشركة. الجداول الجديدة فارغة حاليًا، ولم تُنشأ أو تُعدّل أي فاتورة أو سند أو مبيعات أو أوامر شراء. ستُستعاد الآن موازنة نمار المخططة الموثقة فقط، مع خطوطها الثلاثة عشر.

## مصدر الموازنة المخططة

اللّقطة المحمولة الأقدم لا تحتوي جداول الموازنة التي أضيفت لاحقًا، لذلك استُخرجت موازنة نمار مباشرةً من قاعدة التطوير المرجعية. السجل المستهدف يحمل الكود `NIMAR-2026` ويضم: إيرادًا مخططًا قدره `15,708,739.00` ر.س، وتكلفة مخططة قدرها `13,166,268.00` ر.س، وزكاة مخططة قدرها `63,562.00` ر.س، وربحًا مخططًا قدره `2,478,909.00` ر.س. تشمل خطوطه 13 بندًا، من دون أي تكلفة فعلية أو مستند تشغيلي.

أعيد الاتصال بالخدمة الحية بنجاح بعد تبدل مؤقت في واجهة طرفية Railway. ستُقرأ الآن بطاقات التكلفة ذات المعرفات المرجعية فقط، للتأكد من بقاء الربط بين بنود الموازنة وبطاقاتها صالحًا في قاعدة Railway.

أظهر الفحص أن معرّفات بطاقات التكلفة المرجعية من `90001` إلى `90012` لا توجد في Railway. لن تُنسخ هذه المعرّفات لتجنب روابط يتيمة؛ سيُفحص دليل بطاقات التكلفة الحي لمطابقة الأسماء إن وُجدت، وإلا ستبقى خطوط الموازنة التخطيطية بلا معرّف بطاقة إلى حين استعادة بطاقات التكلفة نفسها عبر عملية منفصلة ومدروسة.

لا يحتوي دليل Railway الحالي على بطاقات تكلفة مطابقة لخطوط نمار؛ الموجود حاليًا هو دليل مواد بناء وبطاقتان عامتان. لذا ستُدخل خطوط موازنة نمار الثلاثة عشر مع حقول `costItemId` فارغة بدل اختراع بطاقات جديدة أو ربطها ببطاقات مواد لا تمثلها. سيبقى هذا الإدخال **تخطيطيًا فقط** ولن ينشئ فاتورة أو سندًا أو حركة فعلية.

## استعادة موازنة نمار

نجحت المعاملة في إنشاء رأس موازنة `NIMAR-2026` برقم `1` وبقيمها المخططة، مع 13 خطًا مخططًا. أعطى فحص أولي انطباعًا خاطئًا بصفر خطوط لأنه لم يتضمن معرّف رأس الموازنة في الاستعلام؛ ثم أكد فحص جدول الخطوط مباشرةً وجود البنود الثلاثة عشر وربطها جميعًا بالموازنة رقم `1`. لم تدخل أي حركات فعلية، وتبقى حقول الربط ببطاقات التكلفة فارغة عمدًا لغياب بطاقات التكلفة المطابقة في Railway.

## تحقق الواجهة الحية

تم فتح لوحة التنفيذ الحية بحساب المالك وتأكيد نجاح النتيجة من واجهة المستخدم نفسها. ظهر مشروع **نمار** في قائمة اختيار المشروع وفي بطاقة صحة المشاريع، وعاد عداد المشاريع النشطة إلى `1`. كما ظهرت بطاقة **الموازنة الشاملة للمشروع** بالقيم المخططة: إيراد `15,708,739` ر.س، تكلفة `13,166,268` ر.س، زكاة `63,562` ر.س، وربح `2,478,909` ر.س. تعرض الواجهة التكلفة الفعلية المسجلة أصلًا (`18,630` ر.س) بشكل منفصل، ولم تُنشأ أي حركة فعلية أثناء الإصلاح.

كما تم التحقق من صفحة المشاريع الحية: `https://erp-app-production-d33a.up.railway.app/projects`. تظهر نمار ضمن سجل المشاريع، وتظهر موازنتها المستقلة بجميع خطوطها الثلاثة عشر وقيمها المخططة الصحيحة. تعرض بطاقات التكلفة في جدول الموازنة بأنها «بند مالي» لأن بطاقات التكلفة المطابقة غير موجودة في Railway، وهو سلوك آمن لا يصنع ارتباطات غير صحيحة.
