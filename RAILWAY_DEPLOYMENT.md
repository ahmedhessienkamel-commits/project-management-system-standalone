# نشر نظام ERP على Railway

## نطاق هذا الدليل

يوضح هذا الدليل تشغيل **نسخة مستقلة** من نظام ERP على Railway مع خدمة MySQL داخل المشروع نفسه. لا يعتمد التشغيل على تسجيل الدخول أو التخزين أو APIs المدمجة الخاصة بالمنصة الأصلية. تعتمد النسخة على Node.js 22، MySQL، وتسجيل دخول محلي بكلمات مرور مشفّرة.

> لا تُنقل بيانات النظام الفعلية من بيئة سابقة تلقائيًا. ابدأ بقاعدة فارغة وأنشئ حساب المالك المحلي، ثم انقل أي بيانات حقيقية فقط بعد مراجعة نطاقها وموافقة المالك.

## الخطوات

| المرحلة | الإجراء | النتيجة المتوقعة |
|---|---|---|
| 1 | ارفع المستودع المستقل إلى GitHub خاص، ثم أضفه في Railway كخدمة من **GitHub Repository**. | تصبح الشفرة مصدر النشر القابل للتدقيق. |
| 2 | اترك Railway يكتشف `Dockerfile` الجذري. | يبني التطبيق عبر `pnpm run build` ويشغّل `dist/index.js`. |
| 3 | أضف خدمة MySQL إلى المشروع نفسه. | تتوفر شبكة داخلية وقيم اتصال مولّدة آليًا. |
| 4 | في متغيرات خدمة التطبيق، اضبط `DATABASE_URL=${{MySQL.MYSQL_URL}}` وبقية المتغيرات التالية. | يتصل التطبيق بقاعدة البيانات الخاصة دون كشفها للإنترنت. |
| 5 | من إعدادات الخدمة، اضبط health check على `/health`. | يتحقق Railway من استجابة الخادم قبل اعتماد النشر. |
| 6 | أنشئ نطاق Railway العام ثم اضبط `APP_URL` عليه. | تعمل روابط الدعوات واستعادة كلمة المرور على النطاق الصحيح. |
| 7 | شغّل الترحيلات ثم أنشئ حساب مالك محلي من Railway Shell. | تصبح قاعدة البيانات والحساب الأول جاهزين. |

توفّر Railway متغير `MYSQL_URL` لخدمة MySQL، ويمكن الرجوع إلى متغير خدمة أخرى بالصيغة `${{SERVICE.VARIABLE}}`؛ لذلك يكون الربط الداخلي الموصى به هو `${{MySQL.MYSQL_URL}}`. تبقى قاعدة البيانات خاصة افتراضيًا، وهو الأنسب للتطبيق داخل المشروع نفسه. [1] [2]

## متغيرات التطبيق في Railway

ضع القيم الحساسة من واجهة المتغيرات فقط، ولا تحفظها في Git أو ملف `.env` منشور.

| المتغير | القيمة أو المبدأ |
|---|---|
| `DATABASE_URL` | `${{MySQL.MYSQL_URL}}` |
| `AUTH_MODE` | `local` |
| `JWT_SECRET` | سر عشوائي جديد وقوي بطول 32 بايت على الأقل |
| `SCHEDULE_SECRET` | سر عشوائي مستقل عن `JWT_SECRET` |
| `APP_URL` | `https://${{RAILWAY_PUBLIC_DOMAIN}}` بعد إنشاء النطاق، أو نطاقك المخصص لاحقًا |
| `VITE_APP_TITLE` | `نظام إدارة المشاريع` |
| `SMTP_*` | اختياري؛ مطلوب فقط لرسائل الدعوات والاستعادة والتنبيهات |
| `BACKUP_EMAIL_RECIPIENT` | اختياري؛ مستلم النسخ التي تُرسل عبر المهمة المجدولة فقط |

## تهيئة قاعدة البيانات والحساب الأول

بعد أن تصبح خدمة التطبيق متاحة في Railway Shell، شغّل الترحيلات أولًا:

```bash
pnpm db:migrate
```

ثم أنشئ المالك المحلي داخل الـShell فقط، وليس في هذه المحادثة أو في Git:

```bash
pnpm local:bootstrap-admin owner@example.com 'كلمة-مرور-قوية-لا-تُشاركها'
```

لا تنسَ تنظيف سجل أوامر الـShell إن أدخلت كلمة المرور كوسيط للأمر. بعد ذلك افتح `/login` وسجّل الدخول بالبريد وكلمة المرور اللذين أنشأتهما.

## البريد والمهام المجدولة

رسائل الدعوات واستعادة كلمة المرور تستخدم SMTP قياسيًا عبر `SMTP_HOST` و`SMTP_PORT` و`SMTP_SECURE` و`SMTP_USER` و`SMTP_PASSWORD` و`SMTP_FROM`. لا يتطلب النظام Gmail أو أي مزود محدد. تبقى المهام المجدولة اختيارية، وهي تستدعي endpoints داخل التطبيق مع الترويسة `x-schedule-secret` المطابقة لـ`SCHEDULE_SECRET`.

لا تفعّل وصولًا عامًا إلى MySQL إلا عند وجود سبب تشغيلي واضح، مثل استيراد محدود من جهاز إداري؛ إذ إن Railway تشير إلى أن الوصول العام ينشئ TCP proxy وقد يرتب كلفة نقل بيانات. [1]

## التحقق بعد النشر

افتح المسارات التالية من النطاق الجديد:

| المسار | المتوقع |
|---|---|
| `/health` | `{"ok":true,"authMode":"local"}` |
| `/login` | واجهة تسجيل الدخول المحلية |
| `/api/trpc/system.health?input=...` | استجابة tRPC الصحية عند استخدامها من الواجهة |

## المراجع

[1]: https://docs.railway.com/databases/mysql "Railway MySQL"
[2]: https://docs.railway.com/variables/reference "Railway Variables Reference"
[3]: https://docs.railway.com/builds/dockerfiles "Railway Dockerfiles"
