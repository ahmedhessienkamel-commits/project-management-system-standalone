# بيانات العينة والبيانات التشغيلية

لم تُضمَّن أي قاعدة بيانات أو تصدير بيانات حقيقي في الحزمة. يحتوي المصدر على مخطط Drizzle والترحيلات اللازمة لإنشاء قاعدة فارغة، ويكفي تشغيل `pnpm db:migrate` ثم `pnpm local:bootstrap-admin` لإنشاء بيئة اختبار آمنة.

هذا القرار مقصود لحماية بيانات المستخدمين وبيانات المشروع الموجودة في البيئة السابقة. لا تنسخ قاعدة الإنتاج أو حسابات المستخدمين أو التجارب القديمة إلى Railway تلقائيًا. إذا لزم نقل بيانات لاحقًا، أنشئ نسخة محمية بعد موافقة المالك، واستعدها أولًا إلى قاعدة منفصلة للتحقق.
