# النسخ الاحتياطي والاستعادة في النسخة المستقلة

## نوعا النسخ المطلوبة

تتضمن الحزمة المستقلة **ترحيلات Drizzle** لبناء قاعدة جديدة، ونسخة بيانات محمولة منفصلة لا تحتوي كلمات المرور أو رموز الاستعادة أو رموز الدعوات. لذلك يجب إنشاء حساب مالك محلي جديد بعد الاستعادة بدل نقل أي كلمة مرور قديمة.

| الغرض | الأداة | المخرَج |
|---|---|---|
| نسخة قواعد بيانات تشغيلية كاملة | `deployment/backup-mysql.sh` | ملف MySQL مضغوط `sql.gz` صالح للاستعادة الكاملة. |
| نقل بيانات آمن للحزمة أو بيئة اختبار | `pnpm export:data` | ملف JSON مضغوط يستبعد الأسرار والرموز. |
| بناء قاعدة جديدة | `pnpm db:migrate` | تطبيق بنية الجداول والترحيلات. |

## تصدير بيانات محمولة

بعد ضبط `DATABASE_URL` على القاعدة المقصودة، نفّذ:

```bash
pnpm export:data exports/erp-portable-data.json.gz
```

لا تستخدم هذه النسخة كبديل لنسخة MySQL الكاملة في حالة الاستعادة من كارثة؛ لأنها تستبعد عمدًا حقول كلمات المرور والرموز. تستخدم لنقل بيانات الأعمال إلى بيئة اختبار أو كأرشيف قابل للمراجعة.

## الاستعادة الكاملة

1. أنشئ قاعدة MySQL فارغة وافتح وصول التطبيق إليها.
2. طبّق الترحيلات باستخدام `pnpm db:migrate` أو الأمر المقابل داخل Docker Compose.
3. استعد ملف `sql.gz` عبر `deployment/restore-mysql.sh`.
4. أنشئ أو أعد تعيين كلمة مرور المالك المحلي:

   ```bash
   pnpm local:bootstrap-admin owner@example.com 'strong-password-at-least-12-characters' 'اسم المالك'
   ```

5. سجّل الدخول محليًا وتحقق من المشاريع والمستخدمين والفواتير والموافقات قبل تحويل DNS إلى الخادم الجديد.

## الحد الأدنى للنسخ الاحتياطي

احتفظ بنسخة يومية مشفرة خارج الخادم، واحتفظ بعدة نقاط زمنية بدل الاستبدال المتكرر لنفس الملف. اختبر الاستعادة ربع سنويًا على قاعدة غير إنتاجية. لا تحفظ كلمات مرور قاعدة البيانات أو ملفات `.env` داخل مجلد النسخ أو المستودع.
