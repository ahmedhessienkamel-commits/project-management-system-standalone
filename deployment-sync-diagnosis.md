# تشخيص مزامنة Railway وGitHub

- صفحة GitHub تؤكد أن الحساب المسجل `ahmedhessienkamel-commits` يملك المستودع الخاص `project-management-system-standalone`.
- أحدث commit ظاهر في المستودع هو `aace666`، بينما النسخة المحلية المعتمدة وصلت إلى `0f88a8d`؛ لذلك لا تحتوي Railway على التعديلات الأحدث إذا كانت متصلة بهذا المستودع.
- محاولة الدفع من Git باستخدام رمز جلسة GitHub التقنية انتهت بخطأ 403 لعدم وجود صلاحية كتابة، رغم نجاح جلسة المتصفح.
- يلزم استبدال وسيلة مزامنة GitHub التقنية أو إعادة ربط خدمة Railway بمصدر الشفرة الحالي قبل إمكان نشر التغييرات على رابط Railway.
- لوحة Railway فُتحت في جلسة المتصفح، لكنها بقيت في حالة تحميل فارغة بلا عناصر تفاعلية أو بيانات مشروع، لذلك لم يمكن فحص الخدمة المرتبطة أو بدء نشر منها من هذه الجلسة.
- أُنشئت صلاحية GitHub مؤقتة لمدة 30 يومًا ومقيدة بالمستودع نفسه فقط، مع إذن محتوى المستودع قراءة وكتابة وإذن البيانات الوصفية المطلوب؛ لا يحتوي هذا السجل على الرمز السري.
- نجح دفع فرع `main` من الإصدار السابق `aace666` إلى الإصدار المعتمد `0f88a8d` في GitHub. مُسحت الحافظة المؤقتة بعد عملية الدفع.
- الرابط الحي `https://erp-app-production-d33a.up.railway.app/` يستجيب ويعرض بوابة تسجيل الدخول المحلية لنظام إدارة المشاريع، ما يؤكد أن خدمة Railway متاحة بعد مزامنة المصدر.
- بعد الدخول إلى الرابط الحي، ما زالت لوحة التنفيذ تعرض موازنة المرحلة القديمة بقيمة 76,911 ر.س. كما أن المتصفح يحمل ملف الواجهة `index-DWgLPBZ-.js` بدل بصمة البناء المحلية الجديدة؛ لذلك لا تكون خدمة Railway قد أعادت النشر من commit `0f88a8d` حتى الآن، رغم نجاح دفعه إلى GitHub.
- إعدادات خدمة `erp-app` في Railway تؤكد اتصال الفرع `main`، لكنها تعرض رسالة `GitHub Repo not found`. كما أظهرت إعدادات النسخ منطقة سابقة غير صالحة منعت النشر، وقد أصبحت المنطقة المعروضة الآن EU West. يلزم إعادة تفويض تطبيق Railway للوصول إلى المستودع ثم بدء نشر جديد.
- أُعيد تثبيت تطبيق Railway GitHub App بصلاحية مقيدة بمستودع `project-management-system-standalone` فقط. لم تعد إعادة التوجيه من GitHub إلى Railway إلى لوحة مرئية، لذلك يلزم التحقق من ربط المصدر من إعدادات خدمة Railway مباشرة.
- نجحت إعادة ربط خدمة `erp-app` بفرع `main` مع تفعيل النشر التلقائي. بدأت Railway بناءً جديدًا من المصدر المحدث؛ ما زالت حالة البناء قيد التنفيذ وقت التسجيل.
- اكتمل بناء خدمة `erp-app` وعادت إلى حالة Online بعد ربط `main`. محاولة فتح الرابط الحي من جلسة المتصفح الحالية انتقلت إلى صفحة فارغة، لذلك يلزم إعادة محاولة مستقلة للتحقق من الواجهة الحية وبصمة ملفاتها.
