# إعداد مهمة تنبيهات الوثائق في Railway

المرجع الرسمي: <https://docs.railway.com/guides/cron-jobs>

- تستخدم Railway تعبير cron من خمسة حقول بتوقيت UTC.
- ستشغّل خدمة cron الأمر `pnpm cron:documents` يوميًا في 06:00 UTC (`0 6 * * *`).
- يجب أن تنهي المهمة تنفيذها بعد الفحص؛ أمر `cron:documents` ينفذ فحصًا واحدًا ثم يخرج.
- ستحتاج خدمة cron منفصلة تستخدم المصدر نفسه ومتغيرات MySQL وSMTP نفسها الخاصة بالخدمة الرئيسية.
