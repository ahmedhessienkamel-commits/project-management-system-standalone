import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { ArrowRight, ShieldCheck, UserCog } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";

const roleLabels: Record<string, string> = { manager: "مدير مشروع", finance: "مالي", input: "إدخال", reviewer: "مراجع", viewer: "مشاهدة" };

export default function Users() {
  const [, setLocation] = useLocation();
  const utils = trpc.useUtils();
  const { data: users = [], isLoading } = trpc.erp.users.list.useQuery();
  const { data: projects = [] } = trpc.erp.projects.list.useQuery();
  const { data: members = [] } = trpc.erp.members.list.useQuery();
  const assign = trpc.erp.members.assign.useMutation({ onSuccess: () => utils.erp.members.list.invalidate() });
  const [form, setForm] = useState({ userId: "", projectId: "", projectRole: "viewer" as "manager" | "finance" | "input" | "reviewer" | "viewer" });
  return <DashboardLayout><div dir="rtl" className="min-h-screen bg-[#f7f8fa] px-4 py-6 sm:px-8 lg:px-10"><div className="mx-auto max-w-7xl space-y-6"><header><Button variant="ghost" className="mb-2 gap-2 px-0 text-slate-500" onClick={() => setLocation("/")}><ArrowRight className="h-4 w-4" /> العودة للوحة التنفيذ</Button><h1 className="text-3xl font-bold text-[#18324b]">المستخدمون والصلاحيات</h1><p className="mt-2 text-sm text-slate-500">إدارة الوصول حسب المشروع والدور، مع إبقاء الاعتماد النهائي للمستخدم المخول.</p></header><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-0 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2 text-lg text-[#18324b]"><UserCog className="h-5 w-5 text-[#b28a3b]" /> إسناد مستخدم لمشروع</CardTitle></CardHeader><CardContent><form className="space-y-4" onSubmit={(e) => { e.preventDefault(); if (!form.userId || !form.projectId) return; assign.mutate({ userId: Number(form.userId), projectId: Number(form.projectId), projectRole: form.projectRole }); }}><SelectField label="المستخدم" value={form.userId} options={users.map((u) => ({ value: String(u.id), label: u.name || u.email || `مستخدم #${u.id}` }))} onChange={(v) => setForm({ ...form, userId: v })} /><SelectField label="المشروع" value={form.projectId} options={projects.map((p) => ({ value: String(p.id), label: p.name }))} onChange={(v) => setForm({ ...form, projectId: v })} /><SelectField label="الدور داخل المشروع" value={form.projectRole} options={Object.entries(roleLabels).map(([value, label]) => ({ value, label }))} onChange={(v) => setForm({ ...form, projectRole: v as typeof form.projectRole })} /><Button className="w-full bg-[#18324b]">حفظ الصلاحية</Button></form></CardContent></Card><Card className="border-0 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2 text-lg text-[#18324b]"><ShieldCheck className="h-5 w-5 text-[#b28a3b]" /> المستخدمون الحاليون</CardTitle></CardHeader><CardContent className="space-y-3">{isLoading ? <div className="py-8 text-center text-sm text-slate-500">جارٍ التحميل...</div> : users.map((user) => <div key={user.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-slate-100 p-3"><div><p className="font-semibold text-[#18324b]">{user.name || "بدون اسم"}</p><p className="text-xs text-slate-500">{user.email || "بدون بريد"}</p></div><Badge variant="outline">{user.role === "admin" ? "إدارة النظام" : "مستخدم"}</Badge></div>)}</CardContent></Card></section><Card className="border-0 shadow-sm"><CardHeader><CardTitle className="text-lg text-[#18324b]">عضويات المشاريع</CardTitle></CardHeader><CardContent className="space-y-2">{members.length ? members.map((member) => <div key={member.id} className="flex justify-between rounded-xl bg-slate-50 p-3 text-sm"><span>مستخدم #{member.userId} · مشروع #{member.projectId}</span><span className="font-semibold text-[#18324b]">{roleLabels[member.projectRole]}</span></div>) : <p className="py-6 text-center text-sm text-slate-400">لم تُسند صلاحيات مشاريع بعد.</p>}</CardContent></Card></div></div></DashboardLayout>;
}
function SelectField({ label, value, options, onChange }: { label: string; value: string; options: { value: string; label: string }[]; onChange: (value: string) => void }) { return <div className="space-y-2"><label className="text-sm text-slate-600">{label}</label><select required value={value} onChange={(e) => onChange(e.target.value)} className="h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm"><option value="">اختر...</option>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></div>; }
