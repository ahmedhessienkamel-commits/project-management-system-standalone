import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { trpc } from "@/lib/trpc";
import { Search, WalletCards } from "lucide-react";
import { useMemo, useState } from "react";

const money = new Intl.NumberFormat("en-US", { maximumFractionDigits: 2 });
const months = ["يناير", "فبراير", "مارس", "أبريل", "مايو", "يونيو", "يوليو", "أغسطس", "سبتمبر", "أكتوبر", "نوفمبر", "ديسمبر"];
const statusLabel: Record<string, string> = { pending: "بانتظار الاعتماد", approved: "معتمدة", rejected: "مرفوضة", cancelled: "ملغاة", scheduled: "مجدول", reserved: "ضمن مسودة مسير", applied: "خُصم من مسير معتمد", deferred: "مؤجل", cancelled_repayment: "ملغى" };

export function EmployeeAdvanceLedger({ employees, me }: { employees: any[]; me: any }) {
  const utils = trpc.useUtils();
  const canManage = me?.role === "admin" || me?.role === "general_manager" || Number(me?.id) === 13170001;
  const [employeeId, setEmployeeId] = useState("");
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState("all");
  const [from, setFrom] = useState("");
  const [to, setTo] = useState("");
  const [form, setForm] = useState({ amount: "", reason: "", repaymentMode: "single" as "single" | "installments", repaymentStartMonth: String(new Date().getMonth() + 1), repaymentStartYear: String(new Date().getFullYear()), installmentCount: "1" });
  const statement = trpc.erp.advanceRequests.statement.useQuery({ employeeId: Number(employeeId || 0) }, { enabled: Boolean(employeeId) && canManage });
  const create = trpc.erp.advanceRequests.create.useMutation({ onSuccess: () => { void statement.refetch(); void utils.erp.advanceRequests.list.invalidate(); setForm((current) => ({ ...current, amount: "", reason: "" })); } });
  const rows = useMemo(() => (statement.data?.advances || []).filter((advance: any) => {
    const text = `${advance.reason || ""} ${advance.status || ""} ${advance.amount || ""}`.toLowerCase();
    const date = String(advance.createdAt || "").slice(0, 10);
    return (!search.trim() || text.includes(search.trim().toLowerCase())) && (status === "all" || advance.status === status) && (!from || date >= from) && (!to || date <= to);
  }), [statement.data, search, status, from, to]);
  if (!canManage) return null;
  const submit = (event: React.FormEvent) => { event.preventDefault(); if (!employeeId || !form.amount || !form.reason.trim()) return; create.mutate({ employeeId: Number(employeeId), amount: Number(form.amount), reason: form.reason.trim(), repaymentMode: form.repaymentMode, repaymentStartMonth: Number(form.repaymentStartMonth), repaymentStartYear: Number(form.repaymentStartYear), installmentCount: form.repaymentMode === "installments" ? Number(form.installmentCount) : 1 }); };
  return <Card className="border-0 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2 text-lg text-[#18324b]"><WalletCards className="h-5 w-5 text-[#b28a3b]" /> كشف حساب سلف الموظفين</CardTitle><p className="text-xs text-slate-500">يعرض أصل كل سلفة وأقساطها وخصوماتها من المسيرات. يمكن إنشاء سلفة كاملة الخصم أو مقسطة؛ ولا تصبح الخصومات نهائية إلا بعد اعتماد المسير.</p></CardHeader><CardContent className="space-y-4"><div className="grid gap-3 rounded-2xl bg-slate-50 p-4 md:grid-cols-4"><div className="space-y-1.5"><Label>الموظف</Label><select value={employeeId} onChange={(event) => setEmployeeId(event.target.value)} className="h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm"><option value="">اختر الموظف</option>{employees.map((employee) => <option key={employee.id} value={employee.id}>{employee.fullName} — {employee.employeeCode}</option>)}</select></div><div className="relative md:col-span-2"><Label>بحث متقدم في كشف السلف</Label><Search className="absolute right-3 top-9 h-4 w-4 text-slate-400" /><Input className="mt-1 pr-9" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="ابحث بالسبب أو الحالة أو المبلغ" /></div><div className="space-y-1.5"><Label>حالة السلفة</Label><select value={status} onChange={(event) => setStatus(event.target.value)} className="h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm"><option value="all">كل الحالات</option><option value="pending">بانتظار الاعتماد</option><option value="approved">معتمدة</option><option value="rejected">مرفوضة</option></select></div><div className="space-y-1.5"><Label>من تاريخ</Label><Input type="date" value={from} onChange={(event) => setFrom(event.target.value)} /></div><div className="space-y-1.5"><Label>إلى تاريخ</Label><Input type="date" value={to} onChange={(event) => setTo(event.target.value)} /></div></div>{employeeId && <><div className="grid gap-3 md:grid-cols-3"><div className="rounded-xl bg-blue-50 p-4"><span className="text-xs text-blue-700">إجمالي السلف المعتمدة</span><p className="mt-1 text-xl font-bold text-[#18324b]">{money.format(Number(statement.data?.totals.grantedAmount || 0))} ر.س</p></div><div className="rounded-xl bg-emerald-50 p-4"><span className="text-xs text-emerald-700">المخصوم من المسيرات</span><p className="mt-1 text-xl font-bold text-[#18324b]">{money.format(Number(statement.data?.totals.appliedAmount || 0))} ر.س</p></div><div className="rounded-xl bg-amber-50 p-4"><span className="text-xs text-amber-700">رصيد السلف المتبقي</span><p className="mt-1 text-xl font-bold text-[#18324b]">{money.format(Number(statement.data?.totals.outstandingAmount || 0))} ر.س</p></div></div><form className="grid gap-3 rounded-2xl border border-amber-100 bg-amber-50/40 p-4 md:grid-cols-4" onSubmit={submit}><div className="space-y-1.5"><Label>قيمة السلفة</Label><Input type="number" min="1" value={form.amount} onChange={(event) => setForm({ ...form, amount: event.target.value })} required /></div><div className="space-y-1.5"><Label>نمط الخصم</Label><select value={form.repaymentMode} onChange={(event) => setForm({ ...form, repaymentMode: event.target.value as "single" | "installments", installmentCount: event.target.value === "installments" ? "2" : "1" })} className="h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm"><option value="single">خصم كامل من مسير واحد</option><option value="installments">تقسيط على عدة مسيرات</option></select></div><div className="space-y-1.5"><Label>شهر بدء الخصم</Label><select value={form.repaymentStartMonth} onChange={(event) => setForm({ ...form, repaymentStartMonth: event.target.value })} className="h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm">{months.map((month, index) => <option key={month} value={index + 1}>{month}</option>)}</select></div><div className="space-y-1.5"><Label>سنة البدء</Label><Input type="number" min="2024" value={form.repaymentStartYear} onChange={(event) => setForm({ ...form, repaymentStartYear: event.target.value })} /></div>{form.repaymentMode === "installments" && <div className="space-y-1.5"><Label>عدد الأقساط</Label><Input type="number" min="2" max="120" value={form.installmentCount} onChange={(event) => setForm({ ...form, installmentCount: event.target.value })} /></div>}<div className="space-y-1.5 md:col-span-2"><Label>سبب السلفة</Label><Input value={form.reason} onChange={(event) => setForm({ ...form, reason: event.target.value })} required placeholder="مثل: سلفة طارئة للموظف" /></div><div className="flex items-end"><Button className="w-full bg-[#18324b]" disabled={create.isPending}>{create.isPending ? "جارٍ حفظ السلفة..." : "إرسال سلفة للاعتماد"}</Button></div></form><div className="space-y-3">{statement.isLoading ? <p className="py-8 text-center text-sm text-slate-500">جارٍ تحميل كشف السلف...</p> : rows.length ? rows.map((advance: any) => <div key={advance.id} className="rounded-xl border border-slate-100 bg-white p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-bold text-[#18324b]">سلفة #{advance.id} — {money.format(Number(advance.amount || 0))} ر.س</p><p className="mt-1 text-xs text-slate-500">{advance.reason} · {String(advance.createdAt || "").slice(0, 10)}</p></div><div className="flex gap-2"><Badge variant="outline">{statusLabel[advance.status] || advance.status}</Badge><Badge className="bg-amber-50 text-amber-800">المتبقي {money.format(Number(advance.outstandingAmount || 0))} ر.س</Badge></div></div><div className="mt-3 grid gap-2 md:grid-cols-3">{(advance.repayments || []).map((repayment: any) => <div key={repayment.id} className="rounded-lg bg-slate-50 p-3 text-xs"><b>{months[Number(repayment.scheduledMonth) - 1]} {repayment.scheduledYear}</b><span className="mx-1">·</span>{money.format(Number(repayment.scheduledAmount || 0))} ر.س<span className="mx-1">·</span><span className="text-slate-500">{statusLabel[repayment.status] || repayment.status}</span></div>)}</div></div>) : <p className="rounded-xl border border-dashed border-slate-200 py-8 text-center text-sm text-slate-500">لا توجد سلف مطابقة لمرشحات البحث الحالية.</p>}</div></>}</CardContent></Card>;
}
